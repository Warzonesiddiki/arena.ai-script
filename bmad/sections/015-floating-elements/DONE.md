# Section 015: Floating Elements (TOC + HUD) — DONE

## Status: ✅ COMPLETE

### Summary
Shared floating-panel behavior for draggable/persistent overlay widgets: the table-of-contents sidebar and the HUD.

### Prior documentation status (before this backfill)
> IMPLEMENTED (FloatingTOC + HUD)

### Verification
- **Syntax:** PASS (`node --check`)
- **Runtime:** PASS (`tests/smoke.js` jsdom boot harness, module reaches 'ready' state)
