{
  "name": "arena-agent-mode-pro",
  "version": "8.0.0",
  "private": true,
  "description": "Arena Agent Mode Pro — a Manifest V3 Chrome extension for transparent, safety-first Arena.ai Agent Mode assistance.",
  "scripts": {
    "clean": "node scripts/clean.cjs",
    "copy:assets": "node scripts/copy-extension-assets.cjs",
    "typecheck": "tsc --noEmit",
    "bundle": "webpack --mode production",
    "bundle:dev": "webpack --mode development",
    "verify:extension": "node tests/extension-scaffold.test.js",
    "build": "npm run clean && npm run typecheck && npm run bundle && npm run copy:assets && npm run verify:extension",
    "build:dev": "npm run clean && npm run typecheck && npm run bundle:dev && npm run copy:assets && npm run verify:extension",
    "watch": "npm run clean && npm run copy:assets && webpack --mode development --watch",
    "test:extension": "npm run build",
    "test:legacy": "node --check arena-agent-mode-pro.user.js && node tests/smoke.js && node tests/regression-toolcall-loop.js && node tests/regression-pause-toggle.js",
    "test": "npm run test:extension && npm run test:unit && npm run test:legacy",
    "test:unit": "jest --coverage --runInBand",
    "audit:runtime": "npm audit --omit=dev",
    "ci": "npm run audit:runtime && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Warzonesiddiki/arena.ai-script.git"
  },
  "keywords": [
    "arena.ai",
    "chrome-extension",
    "manifest-v3",
    "agent-mode"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Warzonesiddiki/arena.ai-script/issues"
  },
  "homepage": "https://github.com/Warzonesiddiki/arena.ai-script#readme",
  "devDependencies": {
    "@types/chrome": "^0.1.31",
    "@types/jest": "^30.0.0",
    "fake-indexeddb": "^6.2.5",
    "jest": "^30.4.2",
    "jest-environment-jsdom": "^30.4.1",
    "jsdom": "^29.0.0",
    "ts-jest": "^29.4.12",
    "ts-loader": "^9.5.4",
    "typescript": "^5.8.3",
    "webpack": "^5.98.0",
    "webpack-cli": "^6.0.1"
  }
}
